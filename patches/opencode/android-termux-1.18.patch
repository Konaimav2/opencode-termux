diff --git a/packages/core/src/cross-spawn-spawner.ts b/packages/core/src/cross-spawn-spawner.ts
index 6ea9022ac..74318e25b 100644
--- a/packages/core/src/cross-spawn-spawner.ts
+++ b/packages/core/src/cross-spawn-spawner.ts
@@ -305,7 +305,8 @@ export const make = Effect.gen(function* () {
 
     return Effect.try({
       try: () => {
-        globalThis.process.kill(-proc.pid!, signal)
+        // Android build: negative-pid (process group) kill panics in Bun.
+        proc.kill(signal)
       },
       catch: (err) => toPlatformError("kill", toError(err), command),
     })
@@ -375,7 +376,8 @@ export const make = Effect.gen(function* () {
               cwd: dir,
               env: env(command.options),
               stdio: stdios(sin, sout, serr, extra),
-              detached: command.options.detached ?? process.platform !== "win32",
+              // Android build: never spawn detached process groups.
+              detached: false,
               shell: command.options.shell,
               windowsHide: process.platform === "win32",
             }),
diff --git a/packages/core/src/filesystem/watcher.ts b/packages/core/src/filesystem/watcher.ts
index c5e206319..2be37b3d5 100644
--- a/packages/core/src/filesystem/watcher.ts
+++ b/packages/core/src/filesystem/watcher.ts
@@ -24,6 +24,9 @@ const SUBSCRIBE_TIMEOUT_MS = 10_000
 export const Event = FileSystemWatcher.Event
 
 const watcher = lazy((): typeof import("@parcel/watcher") | undefined => {
+  // Android build: the @parcel/watcher native binding is not available;
+  // never load it.
+  return undefined
   try {
     const libc = typeof OPENCODE_LIBC === "undefined" ? undefined : OPENCODE_LIBC
     const binding = require(
diff --git a/packages/core/src/global.ts b/packages/core/src/global.ts
index a192a4b46..a163c13ad 100644
--- a/packages/core/src/global.ts
+++ b/packages/core/src/global.ts
@@ -1,5 +1,6 @@
 import path from "path"
 import fs from "fs/promises"
+import { existsSync } from "fs"
 import { xdgData, xdgCache, xdgConfig, xdgState } from "xdg-basedir"
 import os from "os"
 import { Context, Effect, Layer } from "effect"
@@ -8,11 +9,21 @@ import { Flag } from "./flag/flag"
 import { makeGlobalNode } from "./effect/app-node"
 
 const app = "opencode"
-const data = path.join(xdgData!, app)
-const cache = path.join(xdgCache!, app)
-const config = path.join(xdgConfig!, app)
-const state = path.join(xdgState!, app)
-const tmp = path.join(os.tmpdir(), app)
+// Android/Termux builds: Bun standalone starts with an EMPTY process.env on
+// Android/Bionic (getenv/LD_PRELOAD still work at the C level, but JS cannot
+// see any environment variable). Path decisions therefore must not depend on
+// the environment: use os.homedir() (bionic getpwuid fallback — works) and
+// the executable location. Side-by-side test profiles are detected from the
+// executable name instead of XDG_* variables.
+const androidHome = os.homedir()
+const opencodeBin = process.execPath ?? ""
+const isNextProfile = path.basename(opencodeBin).startsWith("opencode-next")
+const profileRoot = isNextProfile ? path.join(androidHome, ".opencode-next") : undefined
+const data = path.join(profileRoot ? path.join(profileRoot, "share") : xdgData!, app)
+const cache = path.join(profileRoot ? path.join(profileRoot, "cache") : path.join(androidHome, ".cache"), app)
+const config = path.join(profileRoot ? path.join(profileRoot, "config") : xdgConfig!, app)
+const state = path.join(profileRoot ? path.join(profileRoot, "state") : xdgState!, app)
+const tmp = path.join(cache, "tmp")
 
 const paths = {
   get home() {
diff --git a/packages/core/src/shell.ts b/packages/core/src/shell.ts
index 29089106d..32e9ba9e7 100644
--- a/packages/core/src/shell.ts
+++ b/packages/core/src/shell.ts
@@ -44,6 +44,12 @@ export async function killTree(proc: ChildProcess, opts?: { exited?: () => boole
     return
   }
 
+  // Android build: negative-pid kill panics in Bun; kill the process directly.
+  proc.kill("SIGTERM")
+  await sleep(SIGKILL_TIMEOUT_MS)
+  if (!opts?.exited?.()) proc.kill("SIGKILL")
+  return
+
   try {
     process.kill(-pid, "SIGTERM")
     await sleep(SIGKILL_TIMEOUT_MS)
diff --git a/packages/opencode/src/config/config.ts b/packages/opencode/src/config/config.ts
index 9e10b67fe..b481e442d 100644
--- a/packages/opencode/src/config/config.ts
+++ b/packages/opencode/src/config/config.ts
@@ -449,6 +449,9 @@ const layer = Layer.effect(
 
           yield* ensureGitignore(dir).pipe(Effect.orDie)
 
+          // Android build: background npm installs into the config dir are
+          // unsupported; plugins are not available in the Termux build.
+          if (false) {
           const dep = yield* npmSvc
             .install(dir, {
               add: [
@@ -469,6 +472,7 @@ const layer = Layer.effect(
               Effect.forkDetach,
             )
           deps.push(dep)
+          }
 
           result.command = mergeDeep(result.command ?? {}, yield* Effect.promise(() => ConfigCommand.load(dir)))
           result.agent = mergeDeep(result.agent ?? {}, yield* Effect.promise(() => ConfigAgent.load(dir)))
diff --git a/packages/tui/src/app.tsx b/packages/tui/src/app.tsx
index 57f372ef7..5d7e25bd2 100644
--- a/packages/tui/src/app.tsx
+++ b/packages/tui/src/app.tsx
@@ -190,8 +190,9 @@ export const run = Effect.fn("Tui.run")(function* (input: TuiInput) {
     Effect.gen(function* () {
       const renderer = yield* Effect.acquireRelease(
         Effect.tryPromise({
-          try: () =>
-            createCliRenderer({
+          try: () => {
+            console.error("[OPENCODE_ANDROID_DIAG] before createCliRenderer")
+            return createCliRenderer({
               externalOutputMode: "passthrough",
               targetFps: 60,
               gatherStats: false,
@@ -203,8 +204,12 @@ export const run = Effect.fn("Tui.run")(function* (input: TuiInput) {
               consoleOptions: {
                 keyBindings: [{ name: "y", ctrl: true, action: "copy-selection" }],
               },
-            }),
-          catch: (error) => (error instanceof Error ? error : new Error(String(error))),
+            })
+          },
+          catch: (error) => {
+            console.error("[OPENCODE_ANDROID_DIAG] createCliRenderer rejected:", error)
+            return error instanceof Error ? error : new Error(String(error))
+          },
         }),
         (renderer) =>
           Effect.sync(() => {
@@ -242,6 +247,7 @@ export const run = Effect.fn("Tui.run")(function* (input: TuiInput) {
         const mode = (await renderer.waitForThemeMode(1000)) ?? "dark"
         if (renderer.isDestroyed) return
 
+        console.error("[OPENCODE_ANDROID_DIAG] before render")
         await render(() => {
           return (
             <ExitProvider
diff --git a/packages/tui/src/audio.ts b/packages/tui/src/audio.ts
index 2f8a0b665..6866632fd 100644
--- a/packages/tui/src/audio.ts
+++ b/packages/tui/src/audio.ts
@@ -5,6 +5,10 @@ let audio: Audio | null | undefined
 const sounds = new Map<string, Promise<AudioSound | null>>()
 
 function getAudio() {
+  // Android build: native audio is stubbed in libopentui.so; never build the
+  // JS Audio wrapper.
+  audio = null
+  return null
   if (audio !== undefined) return audio
   try {
     const next = Audio.create({ autoStart: false })
